Вот полный код **модуля отрисовки графика в терминале (модуль 3)** — файл `s21_graph.c` + заголовочный `s21_graph.h` — с подробными комментариями.

---

## 📁 `s21_graph.h`

```c
#ifndef S21_GRAPH_H
#define S21_GRAPH_H

void draw_graph(double *values, int width, int height, double y_min, double y_max);

#endif  // S21_GRAPH_H
```

---

## 📄 `s21_graph.c`

```c
#include <stdio.h>
#include "s21_graph.h"

// Функция, преобразующая значение y в координату строки
int to_row(double y, int height, double y_min, double y_max) {
    // Если значение выходит за допустимые границы — вернуть -1 (не рисуем)
    if (y < y_min || y > y_max)
        return -1;

    // Линейное преобразование y ∈ [y_min; y_max] в row ∈ [0; height - 1]
    double scale = (y - y_min) / (y_max - y_min);
    int row = height - 1 - (int)(scale * (height - 1) + 0.5);  // округляем вверх
    return row;
}

// Главная функция отрисовки графика
void draw_graph(double *values, int width, int height, double y_min, double y_max) {
    char canvas[height][width];

    // Шаг 1. Инициализация холста точками (.)
    for (int i = 0; i < height; i++)
        for (int j = 0; j < width; j++)
            canvas[i][j] = '.';

    // Шаг 2. Наносим звёздочки (*) на соответствующие строки
    for (int x = 0; x < width; x++) {
        int row = to_row(values[x], height, y_min, y_max);
        if (row >= 0 && row < height)
            canvas[row][x] = '*';
    }

    // Шаг 3. Выводим холст в консоль
    for (int i = 0; i < height; i++) {
        for (int j = 0; j < width; j++) {
            putchar(canvas[i][j]);
        }
        putchar('\n');
    }
}
```

---

## 🔍 Как это работает

- Значения `values[x]` — это уже вычисленные значения `f(x)` (в диапазоне от `y_min` до `y_max`, по заданию — от -1 до 1).
- Они масштабируются на экран **высотой `height`** — 25 строк (по умолчанию).
- Каждая точка `*` рисуется по формуле преобразования `y → row`, где `0` — верх экрана, `height - 1` — низ.
- Остальное пространство заполняется `.` для наглядности.

---

## 🧠 Принципы структурного программирования Эдсгера Дейкстры

| Принцип                         | Реализация                                   |
|----------------------------------|-----------------------------------------------|
| Одна функция — одна задача       | `draw_graph()` занимается только выводом, `to_row()` — только преобразованием |
| Безопасность и проверка границ   | Проверка `row` перед отрисовкой               |
| Читаемость и предсказуемость     | Прямой поток: инициализация → нанесение → вывод |
| Модульность                      | Графика отделена от вычислений                |

---

Если нужно, могу дополнительно написать тестовую функцию, которая вызывает `draw_graph` с тестовыми значениями — для отладки. Продолжим?
